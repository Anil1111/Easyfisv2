<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">
    <link rel="shortcut icon" href="~/Images/icon/easyfislogomini.ico">
    <title>BIR CAS - Sales Journal</title>

    @Styles.Render("~/Content/Software-css")
</head>
<body>
    <div id="o-wrapper" class="o-wrapper">
        <main class="o-content">
            @Html.Partial("_SoftwareHeader")

            <section class="content-header">
                <div class="container">
                    <h3>
                        BIR CAS - Sales Journal
                    </h3>
                    <p>
                        <i class="fa fa-building-o fa-fw"></i> &nbsp; @ViewData["Company"] <br />
                        <i class="fa fa-home fa-fw"></i> &nbsp; <a href="#" onclick="getUserBranches()" style="color: white;">@ViewData["Branch"]</a>
                    </p>
                </div>
            </section>
            <section class="container">
                <div class="panel panel-default">

                    <!--
                        ===============
                        Search / Filter
                        ===============
                    -->
                    <div class="panel-heading">
                        <table style="width: 100%" border="0">
                            <tr>
                                <td style="width: 49.5%">
                                    <div class="input-group">
                                        <span class="input-group-addon"><i class="fa fa-search"></i></span>
                                        <input type="text" class="form-control" id="inpBIRCASSalesJournalSeacrhFilter" placeholder="Search...">
                                    </div>
                                </td>
                                <td style="width: 1%"></td>
                                <td style="width: 24%">
                                    <div class="input-group">
                                        <span class="input-group-addon"><b><i class="fa fa-table"></i></b></span>
                                        <div class="comboBox-wide" id="cboBIRCASSalesJournalDisplayNumberOfRows"></div>
                                    </div>
                                </td>
                                <td style="width: 1%"></td>
                                <td style="width: 24.5%" align="right">
                                    <button class="btn btn-success" onclick="btnBIRCASSalesJournalDownloadCSVOnclick()" id="btnBIRCASSalesJournalDownloadCSV"><i class="fa fa-file-excel-o" aria-hidden="true"></i> CSV</button>
                                    <button class="btn btn-primary" onclick="btnBIRCASSalesJournalPrintPDFOnclick()" id="btnBIRCASSalesJournalPrintPDF"><i class="fa fa-file-pdf-o" aria-hidden="true"></i> PDF</button>
                                    <a href="/Software/BIRCASReports" class="btn btn-danger"><i class="fa fa-close"></i> Close</a>
                                </td>
                            </tr>
                        </table>
                    </div>

                    <!--
                        ======================================
                        Date Range, Company and Branch Filters
                        ======================================
                    -->
                    <div class="panel-heading">
                        <table style="width: 100%" border="0">
                            <tr>
                                <td style="width: 24%">
                                    <div class="input-group">
                                        <span class="input-group-addon"><b>Start Date</b></span>
                                        <div class="comboBox-wide" id="cboBIRCASSalesJournalStartDateFilter"></div>
                                    </div>
                                </td>
                                <td style="width: 1%"></td>
                                <td style="width: 24%" align="right">
                                    <div class="input-group">
                                        <span class="input-group-addon"><b>End Date</b></span>
                                        <div class="comboBox-wide" id="cboBIRCASSalesJournalEndDateFilter"></div>
                                    </div>
                                </td>
                                <td style="width: 1%"></td>
                                <td style="width: 24%">
                                    <div class="input-group">
                                        <span class="input-group-addon"><b>Company</b></span>
                                        <div class="comboBox-wide" id="cboBIRCASSalesJournalCompanyFilter"></div>
                                    </div>
                                </td>
                                <td style="width: 1%"></td>
                                <td style="width: 24%">
                                    <div class="input-group">
                                        <span class="input-group-addon"><b>Branch</b></span>
                                        <div class="comboBox-wide" id="cboBIRCASSalesJournalBranchFilter"></div>
                                    </div>
                                </td>
                            </tr>
                        </table>
                    </div>

                    <!--
                        =========
                        Flex Grid
                        =========
                    -->
                    <div class="panel-body">
                        <div id="BIRCASSalesJournalFlexGrid" class="grid" style="font-size: 12px;"></div>
                    </div>

                    <!--
                        ==================
                        Navigation Buttons
                        ==================
                    -->
                    <div class="panel-footer">
                        <div class="row">
                            <div class="btn-group col-md-7" id="BIRCASSalesJournalNavigationPageGrid">
                                <button type="button" class="btn btn-default border-custom" id="btnBIRCASSalesJournalMoveToFirstPageGrid">
                                    <span class="glyphicon glyphicon-fast-backward"></span>
                                </button>
                                <button type="button" class="btn btn-default border-custom" id="btnBIRCASSalesJournalMoveToPreviousPageGrid">
                                    <span class="glyphicon glyphicon-step-backward"></span>
                                </button>
                                <button type="button" class="btn btn-default border-custom" disabled style="width: 100px" id="btnBIRCASSalesJournalCurrentPageGrid"></button>
                                <button type="button" class="btn btn-default border-custom" id="btnBIRCASSalesJournalMoveToNextPageGrid">
                                    <span class="glyphicon glyphicon-step-forward"></span>
                                </button>
                                <button type="button" class="btn btn-default border-custom" id="btnBIRCASSalesJournalMoveToLastPageGrid">
                                    <span class="glyphicon glyphicon-fast-forward"></span>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <!--
                    ======
                    Totals
                    ======
                -->
                <div class="panel panel-default">
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-md-6"></div>
                            <div class="col-md-6">
                                <dl class="dl-horizontal dl-custom">
                                    <dt>
                                        <b>Total VATable Sales</b>
                                    </dt>
                                    <dd>
                                        <input type="text" class="form-control numberField" id="totalVATableSalesAmount" placeholder="0.00" disabled />
                                    </dd>
                                    <dt>
                                        <b>Total VAT Exempt Sales</b>
                                    </dt>
                                    <dd>
                                        <input type="text" class="form-control numberField" id="totalVATExemptSalesAmount" placeholder="0.00" disabled />
                                    </dd>
                                    <dt>
                                        <b>Total Zero Rated Sales</b>
                                    </dt>
                                    <dd>
                                        <input type="text" class="form-control numberField" id="totalZeroRatedSalesAmount" placeholder="0.00" disabled />
                                    </dd>
                                    <dt>
                                        <b>Total VAT Amount</b>
                                    </dt>
                                    <dd>
                                        <input type="text" class="form-control numberField" id="totalVATAmount" placeholder="0.00" disabled />
                                    </dd>
                                    <dt>
                                        <b>Total Discount</b>
                                    </dt>
                                    <dd>
                                        <input type="text" class="form-control numberField" id="totalDiscountAmount" placeholder="0.00" disabled />
                                    </dd>
                                    <dt>
                                        <b>Total Amount</b>
                                    </dt>
                                    <dd>
                                        <input type="text" class="form-control numberField" id="totalAmount" placeholder="0.00" disabled />
                                    </dd>
                                </dl>
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            @Html.Partial("_SoftwareFooter")
        </main>
    </div>
    <div id="c-mask" class="c-mask"></div>

    @Scripts.Render("~/Scripts/Software-js")
    <script type="text/javascript">
        // ==================
        // Data Range Filters
        // ==================
        var cboBIRCASSalesJournalStartDateFilter = new wijmo.input.InputDate('#cboBIRCASSalesJournalStartDateFilter');
        var cboBIRCASSalesJournalEndDateFilter = new wijmo.input.InputDate('#cboBIRCASSalesJournalEndDateFilter');
        var isCboBIRCASSalesJournalStartDateFilterSelected = false;
        var isCboBIRCASSalesJournalEndDateFilterSelected = false;

        // ==========================
        // Company and Branch Filters
        // ==========================
        var cboBIRCASSalesJournalCompanyFilter = new wijmo.input.ComboBox('#cboBIRCASSalesJournalCompanyFilter');
        var isCboBIRCASSalesJournalCompanyFilterSelected = false;
        var cboBIRCASSalesJournalBranchFilter = new wijmo.input.ComboBox('#cboBIRCASSalesJournalBranchFilter');

        // =============================
        // Collection View and Flex Grid
        // =============================
        var BIRCASSalesJournalCollectionView;
        var BIRCASSalesJournalFlexGrid;
        var cboBIRCASSalesJournalDisplayNumberOfRows = new wijmo.input.ComboBox('#cboBIRCASSalesJournalDisplayNumberOfRows');

        // ==================
        // Navigation Buttons
        // ==================
        var btnBIRCASSalesJournalMoveToFirstPageGrid = document.getElementById('btnBIRCASSalesJournalMoveToFirstPageGrid');
        var btnBIRCASSalesJournalMoveToPreviousPageGrid = document.getElementById('btnBIRCASSalesJournalMoveToPreviousPageGrid');
        var btnBIRCASSalesJournalMoveToNextPageGrid = document.getElementById('btnBIRCASSalesJournalMoveToNextPageGrid');
        var btnBIRCASSalesJournalMoveToLastPageGrid = document.getElementById('btnBIRCASSalesJournalMoveToLastPageGrid');
        var btnBIRCASSalesJournalCurrentPageGrid = document.getElementById('btnBIRCASSalesJournalCurrentPageGrid');
        var BIRCASSalesJournalCurrentPageIndex = 0;

        // =======================
        // Get URL Parameter Value
        // =======================
        function getURLParameterValue(name) {
            name = name.replace(/[\[]/, "\\\[").replace(/[\]]/, "\\\]");

            var regexS = "[\\?&]" + name + "=([^&#]*)";
            var regex = new RegExp(regexS);

            var results = regex.exec(window.location.href);
            if (results == null) {
                return "";
            } else {
                return results[1];
            }
        }

        // ================================
        // Create Combo Date Ranged Filters
        // ================================
        function createCboDateRangedFilter() {
            NProgress.start();
            cboBIRCASSalesJournalStartDateFilter.dispose();
            cboBIRCASSalesJournalStartDateFilter = new wijmo.input.InputDate('#cboBIRCASSalesJournalStartDateFilter', {
                format: 'MM-dd-yyyy',
                value: new Date(getURLParameterValue("StartDate")),
                onValueChanged: function () {
                    if (!isCboBIRCASSalesJournalStartDateFilterSelected) {
                        NProgress.start();
                        refreshBIRCASSalesJournalFlexGrid();
                    } else {
                        isCboBIRCASSalesJournalStartDateFilterSelected = false;
                    }
                }
            });

            cboBIRCASSalesJournalEndDateFilter.dispose();
            cboBIRCASSalesJournalEndDateFilter = new wijmo.input.InputDate('#cboBIRCASSalesJournalEndDateFilter', {
                format: 'MM-dd-yyyy',
                value: new Date(getURLParameterValue("EndDate")),
                onValueChanged: function () {
                    if (!isCboBIRCASSalesJournalEndDateFilterSelected) {
                        NProgress.start();
                        refreshBIRCASSalesJournalFlexGrid();
                    } else {
                        isCboBIRCASSalesJournalEndDateFilterSelected = false;
                    }
                }
            });

            createCboCompanyFilter();
        }

        // ===========================
        // Create Combo Company Filter
        // ===========================
        function createCboCompanyFilter() {
            var companyObservableArray = new wijmo.collections.ObservableArray();
            $.ajax({
                url: '/api/BIRCASSalesJournal/dropdown/list/company',
                cache: false,
                type: 'GET',
                contentType: 'application/json; charset=utf-8',
                success: function (results) {
                    if (results.length > 0) {
                        for (i = 0; i < results.length; i++) {
                            companyObservableArray.push({
                                Id: results[i]["Id"],
                                Company: results[i]["Company"]
                            });
                        }
                    }

                    var disabled = true;
                    if (results.length > 0) {
                        disabled = false;
                    } else {
                        $("#btnBIRCASSalesJournalDownloadCSV").prop("disabled", true);
                        $("#btnBIRCASSalesJournalPrintPDF").prop("disabled", true);
                    }

                    cboBIRCASSalesJournalCompanyFilter.dispose();
                    cboBIRCASSalesJournalCompanyFilter = new wijmo.input.ComboBox('#cboBIRCASSalesJournalCompanyFilter', {
                        disabled: disabled,
                        isEditable: false,
                        itemsSource: companyObservableArray,
                        displayMemberPath: "Company",
                        selectedValuePath: "Id",
                        selectedValue: getURLParameterValue("CompanyId"),
                        onSelectedIndexChanged: function () {
                            NProgress.start();
                            $("#btnBIRCASSalesJournalDownloadCSV").prop("disabled", false);
                            $("#btnBIRCASSalesJournalPrintPDF").prop("disabled", false);
                            isCboBIRCASSalesJournalCompanyFilterSelected = true;
                            createCboBranchFilter();
                        }
                    });

                    createCboBranchFilter();
                }
            });
        }

        // ==========================
        // Create Combo Branch Filter
        // ==========================
        function createCboBranchFilter() {
            var branchObservableArray = new wijmo.collections.ObservableArray();

            if (cboBIRCASSalesJournalCompanyFilter.selectedValue != null) {
                $.ajax({
                    url: '/api/BIRCASSalesJournal/dropdown/list/branch/' + cboBIRCASSalesJournalCompanyFilter.selectedValue,
                    cache: false,
                    type: 'GET',
                    contentType: 'application/json; charset=utf-8',
                    success: function (results) {
                        if (results.length > 0) {
                            branchObservableArray.push({
                                Id: 0,
                                Branch: "All Branches"
                            });

                            for (i = 0; i < results.length; i++) {
                                branchObservableArray.push({
                                    Id: results[i]["Id"],
                                    Branch: results[i]["Branch"]
                                });
                            }
                        }

                        var disabled = true;
                        if (results.length > 0) {
                            disabled = false;
                        } else {
                            $("#btnBIRCASSalesJournalDownloadCSV").prop("disabled", true);
                            $("#btnBIRCASSalesJournalPrintPDF").prop("disabled", true);
                        }

                        cboBIRCASSalesJournalBranchFilter.dispose();
                        cboBIRCASSalesJournalBranchFilter = new wijmo.input.ComboBox('#cboBIRCASSalesJournalBranchFilter', {
                            disabled: disabled,
                            isEditable: false,
                            itemsSource: branchObservableArray,
                            displayMemberPath: "Branch",
                            selectedValuePath: "Id",
                            selectedValue: getURLParameterValue("BranchId"),
                            onSelectedIndexChanged: function () {
                                NProgress.start();
                                refreshBIRCASSalesJournalFlexGrid();
                            }
                        });

                        createCboDisplayNumberOfRows();
                    }
                });
            }
        }

        // ===================================
        // Create Combo Display Number of Rows
        // ===================================
        function createCboDisplayNumberOfRows() {
            var rowsObservableArray = new wijmo.collections.ObservableArray();

            for (var i = 0; i <= 4; i++) {
                var rows = 0;
                var rowsString = "";

                if (i == 0) {
                    rows = 15;
                    rowsString = "Show 15 Rows";
                } else if (i == 1) {
                    rows = 50;
                    rowsString = "Show 50 Rows";
                } else if (i == 2) {
                    rows = 100;
                    rowsString = "Show 100 Rows";
                } else if (i == 3) {
                    rows = 150;
                    rowsString = "Show 150 Rows";
                } else {
                    rows = 200;
                    rowsString = "Show 200 Rows";
                }

                rowsObservableArray.push({
                    rowNumber: rows,
                    rowString: rowsString
                });
            }

            cboBIRCASSalesJournalDisplayNumberOfRows.dispose();
            cboBIRCASSalesJournalDisplayNumberOfRows = new wijmo.input.ComboBox('#cboBIRCASSalesJournalDisplayNumberOfRows', {
                isEditable: false,
                itemsSource: rowsObservableArray,
                displayMemberPath: "rowString",
                selectedValuePath: "rowNumber",
                selectedValue: 50,
                onSelectedIndexChanged: function () {
                    NProgress.start();

                    setTimeout(function () {
                        var numberOfRows = cboBIRCASSalesJournalDisplayNumberOfRows.selectedValue;
                        BIRCASSalesJournalCollectionView.pageSize = parseInt(numberOfRows);
                        BIRCASSalesJournalCollectionView.refresh();
                        BIRCASSalesJournalFlexGrid.refresh();

                        NProgress.done();
                    }, 300);
                }
            });

            if (!isCboBIRCASSalesJournalCompanyFilterSelected) {
                createBIRCASSalesJournalFlexGrid();
            } else {
                refreshBIRCASSalesJournalFlexGrid();
            }
        }

        // ========
        // Get Data
        // ========
        function getBIRCASSalesJournalData() {
            var BIRCASSalesJournalObservableArray = new wijmo.collections.ObservableArray;
            if (cboBIRCASSalesJournalCompanyFilter.selectedValue != null) {
                if (cboBIRCASSalesJournalBranchFilter.selectedValue != null) {
                    var startDate = cboBIRCASSalesJournalStartDateFilter.value.toLocaleDateString().replace(/\//g, '-');
                    var endDate = cboBIRCASSalesJournalEndDateFilter.value.toLocaleDateString().replace(/\//g, '-');
                    var companyId = cboBIRCASSalesJournalCompanyFilter.selectedValue;
                    var branchId = cboBIRCASSalesJournalBranchFilter.selectedValue;

                    $.ajax({
                        url: '/api/BIRCASSalesJournal/list/' + startDate + '/' + endDate + '/' + + companyId + '/' + branchId,
                        cache: false,
                        type: 'GET',
                        contentType: 'application/json; charset=utf-8',
                        success: function (results) {

                            var totalDiscountAmount = 0;
                            var totalAmount = 0;
                            var totalVATableSalesAmount = 0;
                            var totalVATExemptSalesAmount = 0;
                            var totalZeroRatedSalesAmount = 0;
                            var totalVATAmount = 0;

                            if (results.length > 0) {
                                for (i = 0; i < results.length; i++) {
                                    var dataDateResult = new Date(results[i]["Date"]);
                                    var dataDate = ('0' + (dataDateResult.getMonth() + 1)).slice(-2) + '-' + ('0' + dataDateResult.getDate()).slice(-2) + '-' + dataDateResult.getFullYear();

                                    BIRCASSalesJournalObservableArray.push({
                                        Date: dataDate,
                                        ReferenceNumber: results[i]["ReferenceNumber"],
                                        Customer: results[i]["Customer"],
                                        CustomerTIN: results[i]["CustomerTIN"],
                                        Address: results[i]["Address"],
                                        DocumentReference: results[i]["DocumentReference"],
                                        ManualReferenceNumber: results[i]["ManualReferenceNumber"],
                                        DiscountAmount: results[i]["DiscountAmount"],
                                        DiscountAmountDisplay: parseFloat(results[i]["DiscountAmount"]).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ","),
                                        Amount: results[i]["Amount"],
                                        AmountDisplay: parseFloat(results[i]["Amount"]).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ","),
                                        VATableSalesAmount: results[i]["VATableSalesAmount"],
                                        VATableSalesAmountDisplay: parseFloat(results[i]["VATableSalesAmount"]).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ","),
                                        VATExemptSalesAmount: results[i]["VATExemptSalesAmount"],
                                        VATExemptSalesAmountDisplay: parseFloat(results[i]["VATExemptSalesAmount"]).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ","),
                                        ZeroRatedSalesAmount: results[i]["ZeroRatedSalesAmount"],
                                        ZeroRatedSalesAmountDisplay: parseFloat(results[i]["ZeroRatedSalesAmount"]).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ","),
                                        VATAmount: results[i]["VATAmount"],
                                        VATAmountDisplay: parseFloat(results[i]["VATAmount"]).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ","),
                                    });

                                    totalDiscountAmount += results[i]["DiscountAmount"];
                                    totalAmount += results[i]["Amount"];
                                    totalVATableSalesAmount += results[i]["VATableSalesAmount"];
                                    totalVATExemptSalesAmount += results[i]["VATExemptSalesAmount"];
                                    totalZeroRatedSalesAmount += results[i]["ZeroRatedSalesAmount"];
                                    totalVATAmount += results[i]["VATAmount"];
                                }
                            }

                            document.getElementById("totalDiscountAmount").value = parseFloat(totalDiscountAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                            document.getElementById("totalAmount").value = parseFloat(totalAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                            document.getElementById("totalVATableSalesAmount").value = parseFloat(totalVATableSalesAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                            document.getElementById("totalVATExemptSalesAmount").value = parseFloat(totalVATExemptSalesAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                            document.getElementById("totalZeroRatedSalesAmount").value = parseFloat(totalZeroRatedSalesAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                            document.getElementById("totalVATAmount").value = parseFloat(totalVATAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                            NProgress.done();
                        }
                    });
                } else {
                    document.getElementById("totalDiscountAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATableSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATExemptSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalZeroRatedSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    NProgress.done();
                }
            } else {
                document.getElementById("totalDiscountAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                document.getElementById("totalAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                document.getElementById("totalVATableSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                document.getElementById("totalVATExemptSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                document.getElementById("totalZeroRatedSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                document.getElementById("totalVATAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                NProgress.done();
            }

            return BIRCASSalesJournalObservableArray;
        }

        // ================
        // Create Flex Grid
        // ================
        function createBIRCASSalesJournalFlexGrid() {
            BIRCASSalesJournalCollectionView = new wijmo.collections.CollectionView(getBIRCASSalesJournalData());
            BIRCASSalesJournalCollectionView.canFilter = true;
            BIRCASSalesJournalCollectionView.pageSize = cboBIRCASSalesJournalDisplayNumberOfRows.selectedValue;

            var filterText = '';
            $('#inpBIRCASSalesJournalSeacrhFilter').keyup(function () {
                filterText = this.value.toLowerCase();
                BIRCASSalesJournalCollectionView.refresh();

                if (BIRCASSalesJournalCollectionView.items.length > 0) {
                    var totalDiscountAmount = 0;
                    var totalAmount = 0;
                    var totalVATableSalesAmount = 0;
                    var totalVATExemptSalesAmount = 0;
                    var totalZeroRatedSalesAmount = 0;
                    var totalVATAmount = 0;

                    for (var p = 1; p <= BIRCASSalesJournalCollectionView.pageCount; p++) {
                        for (i = 0; i < BIRCASSalesJournalCollectionView.items.length; i++) {
                            totalDiscountAmount += BIRCASSalesJournalCollectionView.items[i].DiscountAmount;
                            totalAmount += BIRCASSalesJournalCollectionView.items[i].Amount;
                            totalVATableSalesAmount += BIRCASSalesJournalCollectionView.items[i].VATableSalesAmount;
                            totalVATExemptSalesAmount += BIRCASSalesJournalCollectionView.items[i].VATExemptSalesAmount;
                            totalZeroRatedSalesAmount += BIRCASSalesJournalCollectionView.items[i].ZeroRatedSalesAmount;
                            totalVATAmount += BIRCASSalesJournalCollectionView.items[i].VATAmount;
                        }

                        BIRCASSalesJournalCollectionView.moveToNextPage();
                        if (p == BIRCASSalesJournalCollectionView.pageCount) {
                            BIRCASSalesJournalCollectionView.moveToPage(BIRCASSalesJournalCurrentPageIndex - 1);
                        }
                    }

                    document.getElementById("totalDiscountAmount").value = parseFloat(totalDiscountAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalAmount").value = parseFloat(totalAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATableSalesAmount").value = parseFloat(totalVATableSalesAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATExemptSalesAmount").value = parseFloat(totalVATExemptSalesAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalZeroRatedSalesAmount").value = parseFloat(totalZeroRatedSalesAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATAmount").value = parseFloat(totalVATAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                } else {
                    document.getElementById("totalDiscountAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATableSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATExemptSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalZeroRatedSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                }
            });

            BIRCASSalesJournalCollectionView.filter = function (item) {
                return !filterText || (item.ReferenceNumber.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.Customer.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.CustomerTIN.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.Address.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.DocumentReference.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.ManualReferenceNumber.toLowerCase().indexOf(filterText) > -1);
            }

            BIRCASSalesJournalCollectionView.collectionChanged.addHandler(function (sender, args) {
                updateBIRCASSalesJournalNavigationButton();
            });

            BIRCASSalesJournalFlexGrid = new wijmo.grid.FlexGrid('#BIRCASSalesJournalFlexGrid');
            BIRCASSalesJournalFlexGrid.initialize({
                columns: [
                    {
                        "header": "Date",
                        "binding": "Date",
                        "allowSorting": true,
                        "width": "1.5*"
                    },
                    {
                        "header": "Tx No.",
                        "binding": "ReferenceNumber",
                        "allowSorting": true,
                        "width": "2*"
                    },
                    {
                        "header": "Customer",
                        "binding": "Customer",
                        "allowSorting": true,
                        "width": "2*"
                    },
                    {
                        "header": "TIN",
                        "binding": "CustomerTIN",
                        "allowSorting": true,
                        "width": "2*"
                    },
                    {
                        "header": "Address",
                        "binding": "Address",
                        "allowSorting": true,
                        "width": "3*"
                    },
                    {
                        "header": "Doc. Ref. No.",
                        "binding": "DocumentReference",
                        "allowSorting": true,
                        "width": "2*"
                    },
                    {
                        "header": "SI Ref. No.",
                        "binding": "ManualReferenceNumber",
                        "allowSorting": true,
                        "width": "2*"
                    },
                    {
                        "header": "VATable Sales",
                        "binding": "VATableSalesAmountDisplay",
                        "allowSorting": true,
                        "width": "2*",
                        "align": "right"
                    },
                    {
                        "header": "VAT Exempt Sales",
                        "binding": "VATExemptSalesAmountDisplay",
                        "allowSorting": true,
                        "width": "2*",
                        "align": "right"
                    },
                    {
                        "header": "Zero Rated Sales",
                        "binding": "ZeroRatedSalesAmountDisplay",
                        "allowSorting": true,
                        "width": "2*",
                        "align": "right"
                    },
                    {
                        "header": "VAT",
                        "binding": "VATAmountDisplay",
                        "allowSorting": true,
                        "width": "2*",
                        "align": "right"
                    },
                    {
                        "header": "Discount",
                        "binding": "DiscountAmountDisplay",
                        "allowSorting": true,
                        "width": "2*",
                        "align": "right"
                    },
                    {
                        "header": "Amount",
                        "binding": "AmountDisplay",
                        "allowSorting": true,
                        "width": "2*",
                        "align": "right"
                    },
                ],
                autoGenerateColumns: false,
                itemsSource: BIRCASSalesJournalCollectionView,
                isReadOnly: true,
                autoSizeMode: wijmo.grid.AutoSizeMode.Both,
                allowDragging: wijmo.grid.AllowDragging.None,
                selectionMode: wijmo.grid.SelectionMode.Row
            });

            BIRCASSalesJournalFlexGrid.trackChanges = true;

            updateBIRCASSalesJournalNavigationButton();

            btnBIRCASSalesJournalMoveToFirstPageGrid.addEventListener('click', function () {
                BIRCASSalesJournalCollectionView.moveToFirstPage();
                BIRCASSalesJournalCurrentPageIndex = BIRCASSalesJournalCollectionView.pageIndex + 1;
                updateBIRCASSalesJournalNavigationButton();
            });

            btnBIRCASSalesJournalMoveToPreviousPageGrid.addEventListener('click', function () {
                BIRCASSalesJournalCollectionView.moveToPreviousPage();
                BIRCASSalesJournalCurrentPageIndex = BIRCASSalesJournalCollectionView.pageIndex + 1;
                updateBIRCASSalesJournalNavigationButton();
            });

            btnBIRCASSalesJournalMoveToNextPageGrid.addEventListener('click', function () {
                BIRCASSalesJournalCollectionView.moveToNextPage();
                BIRCASSalesJournalCurrentPageIndex = BIRCASSalesJournalCollectionView.pageIndex + 1;
                updateBIRCASSalesJournalNavigationButton();
            });

            btnBIRCASSalesJournalMoveToLastPageGrid.addEventListener('click', function () {
                BIRCASSalesJournalCollectionView.moveToLastPage();
                BIRCASSalesJournalCurrentPageIndex = BIRCASSalesJournalCollectionView.pageIndex + 1;
                updateBIRCASSalesJournalNavigationButton();
            });
        }

        // =========================
        // Update Navigation Buttons
        // =========================
        function updateBIRCASSalesJournalNavigationButton() {
            if (BIRCASSalesJournalCollectionView.pageSize <= 0) {
                document.getElementById('BIRCASSalesJournalNavigationPageGrid').style.display = 'none';
                return;
            }
            document.getElementById('BIRCASSalesJournalNavigationPageGrid').style.display = 'block';
            if (BIRCASSalesJournalCollectionView.pageIndex === 0) {
                btnBIRCASSalesJournalMoveToFirstPageGrid.setAttribute('disabled', 'disabled');
                btnBIRCASSalesJournalMoveToPreviousPageGrid.setAttribute('disabled', 'disabled');
                btnBIRCASSalesJournalMoveToNextPageGrid.removeAttribute('disabled');
                btnBIRCASSalesJournalMoveToLastPageGrid.removeAttribute('disabled');
            } else if (BIRCASSalesJournalCollectionView.pageIndex === (BIRCASSalesJournalCollectionView.pageCount - 1)) {
                btnBIRCASSalesJournalMoveToFirstPageGrid.removeAttribute('disabled');
                btnBIRCASSalesJournalMoveToPreviousPageGrid.removeAttribute('disabled');
                btnBIRCASSalesJournalMoveToNextPageGrid.setAttribute('disabled', 'disabled');
                btnBIRCASSalesJournalMoveToLastPageGrid.setAttribute('disabled', 'disabled');
            } else {
                btnBIRCASSalesJournalMoveToFirstPageGrid.removeAttribute('disabled');
                btnBIRCASSalesJournalMoveToPreviousPageGrid.removeAttribute('disabled');
                btnBIRCASSalesJournalMoveToNextPageGrid.removeAttribute('disabled');
                btnBIRCASSalesJournalMoveToLastPageGrid.removeAttribute('disabled');
            }
            btnBIRCASSalesJournalCurrentPageGrid.innerHTML = (BIRCASSalesJournalCollectionView.pageIndex + 1) + ' / ' + BIRCASSalesJournalCollectionView.pageCount;
        }

        // =================
        // Refresh Flex Grid
        // =================
        function refreshBIRCASSalesJournalFlexGrid() {
            BIRCASSalesJournalCollectionView = new wijmo.collections.CollectionView(getBIRCASSalesJournalData());
            BIRCASSalesJournalCollectionView.canFilter = true;
            BIRCASSalesJournalCollectionView.pageSize = cboBIRCASSalesJournalDisplayNumberOfRows.selectedValue;

            var filterText = '';
            $('#inpBIRCASSalesJournalSeacrhFilter').keyup(function () {
                filterText = this.value.toLowerCase();
                BIRCASSalesJournalCollectionView.refresh();

                if (BIRCASSalesJournalCollectionView.items.length > 0) {
                    var totalDiscountAmount = 0;
                    var totalAmount = 0;
                    var totalVATableSalesAmount = 0;
                    var totalVATExemptSalesAmount = 0;
                    var totalZeroRatedSalesAmount = 0;
                    var totalVATAmount = 0;

                    for (var p = 1; p <= BIRCASSalesJournalCollectionView.pageCount; p++) {
                        for (i = 0; i < BIRCASSalesJournalCollectionView.items.length; i++) {
                            totalDiscountAmount += BIRCASSalesJournalCollectionView.items[i].DiscountAmount;
                            totalAmount += BIRCASSalesJournalCollectionView.items[i].Amount;
                            totalVATableSalesAmount += BIRCASSalesJournalCollectionView.items[i].VATableSalesAmount;
                            totalVATExemptSalesAmount += BIRCASSalesJournalCollectionView.items[i].VATExemptSalesAmount;
                            totalZeroRatedSalesAmount += BIRCASSalesJournalCollectionView.items[i].ZeroRatedSalesAmount;
                            totalVATAmount += BIRCASSalesJournalCollectionView.items[i].VATAmount;
                        }

                        BIRCASSalesJournalCollectionView.moveToNextPage();
                        if (p == BIRCASSalesJournalCollectionView.pageCount) {
                            BIRCASSalesJournalCollectionView.moveToPage(BIRCASSalesJournalCurrentPageIndex - 1);
                        }
                    }

                    document.getElementById("totalDiscountAmount").value = parseFloat(totalDiscountAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalAmount").value = parseFloat(totalAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATableSalesAmount").value = parseFloat(totalVATableSalesAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATExemptSalesAmount").value = parseFloat(totalVATExemptSalesAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalZeroRatedSalesAmount").value = parseFloat(totalZeroRatedSalesAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATAmount").value = parseFloat(totalVATAmount).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                } else {
                    document.getElementById("totalDiscountAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATableSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATExemptSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalZeroRatedSalesAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                    document.getElementById("totalVATAmount").value = parseFloat(0).toFixed(2).toLocaleString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
                }
            });

            BIRCASSalesJournalCollectionView.filter = function (item) {
                return !filterText || (item.ReferenceNumber.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.Customer.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.CustomerTIN.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.Address.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.DocumentReference.toLowerCase().indexOf(filterText) > -1)
                    || !filterText || (item.ManualReferenceNumber.toLowerCase().indexOf(filterText) > -1);
            }

            BIRCASSalesJournalCollectionView.collectionChanged.addHandler(function (sender, args) {
                updateBIRCASSalesJournalNavigationButton();
            });

            BIRCASSalesJournalFlexGrid.itemsSource = BIRCASSalesJournalCollectionView;
            BIRCASSalesJournalFlexGrid.trackChanges = true;
        }

        // ============
        // Download CSV
        // ============
        function btnBIRCASSalesJournalDownloadCSVOnclick() {
            var CSV = '';
            var BIRCASSalesJournal = [];
            var fileName = 'BIRCASSalesJournal.CSV';

            BIRCASSalesJournalCollectionView.moveToFirstPage();

            for (var p = 1; p <= BIRCASSalesJournalCollectionView.pageCount; p++) {
                for (i = 0; i < BIRCASSalesJournalCollectionView.items.length; i++) {
                    BIRCASSalesJournal.push({
                        Date: BIRCASSalesJournalCollectionView.items[i].Date,
                        ReferenceNumber: BIRCASSalesJournalCollectionView.items[i].ReferenceNumber,
                        Customer: BIRCASSalesJournalCollectionView.items[i].Customer,
                        CustomerTIN: BIRCASSalesJournalCollectionView.items[i].CustomerTIN,
                        Address: BIRCASSalesJournalCollectionView.items[i].Address,
                        DocumentReference: BIRCASSalesJournalCollectionView.items[i].DocumentReference,
                        ManualReferenceNumber: BIRCASSalesJournalCollectionView.items[i].ManualReferenceNumber,
                        VATableSalesAmount: BIRCASSalesJournalCollectionView.items[i].VATableSalesAmount,
                        VATExemptSalesAmount: BIRCASSalesJournalCollectionView.items[i].VATExemptSalesAmount,
                        ZeroRatedSalesAmount: BIRCASSalesJournalCollectionView.items[i].ZeroRatedSalesAmount,
                        VATAmount: BIRCASSalesJournalCollectionView.items[i].VATAmount,
                        DiscountAmount: BIRCASSalesJournalCollectionView.items[i].DiscountAmount,
                        Amount: BIRCASSalesJournalCollectionView.items[i].Amount,
                    });
                }

                BIRCASSalesJournalCollectionView.moveToNextPage();
                if (p == BIRCASSalesJournalCollectionView.pageCount) {
                    BIRCASSalesJournalCollectionView.moveToPage(BIRCASSalesJournalCurrentPageIndex - 1);
                }
            }

            var Headers = [];
            Headers.push({
                Date: "Date",
                ReferenceNumber: "Tx No.",
                Customer: "Customer",
                CustomerTIN: "TIN",
                Address: "Address",
                DocumentReference: "Doc. Ref. No.",
                ManualReferenceNumber: "SI Ref. No.",
                VATableSalesAmount: "VATable Sales",
                VATExemptSalesAmount: "VAT Exempt Sales",
                ZeroRatedSalesAmount: "Zero Rated Sales",
                VATAmount: "VAT",
                DiscountAmount: "Discount",
                Amount: "Amount",
            });

            var header = '';
            for (var b in Headers[0]) {
                header += '"' + Headers[0][b] + '",';
            }

            CSV = header + '\r\n';
            for (var i = 0; i < BIRCASSalesJournal.length; i++) {
                var row = '';
                for (var a in BIRCASSalesJournal[i]) {
                    row += '"' + BIRCASSalesJournal[i][a] + '",';
                }

                row.slice(0, row.length - 1);
                CSV += row + '\r\n';
            }

            if (CSV == '') {
                toastr.error("No data.");
            } else {
                var link = document.createElement("a");
                if (link.download !== undefined) {
                    var blob = new Blob([CSV], { type: 'text/csv;charset=utf-8;' });
                    var url = URL.createObjectURL(blob);
                    link.setAttribute("href", url);
                    link.setAttribute("download", fileName);
                    link.style = "visibility:hidden";
                }

                if (navigator.msSaveBlob) {
                    link.addEventListener("click", function (event) {
                        var blob = new Blob([CSV], {
                            "type": "text/csv;charset=utf-8;"
                        });
                        navigator.msSaveBlob(blob, fileName);
                    }, false);
                }

                document.body.appendChild(link);
                link.click();
                document.body.removeChild(link);

                toastr.success("Dowload successful.");
            }
        }

        // =====================
        // Preview and Print PDF
        // =====================
        function btnBIRCASSalesJournalPrintPDFOnclick() {
            if (cboBIRCASSalesJournalCompanyFilter.selectedValue != null) {
                if (cboBIRCASSalesJournalBranchFilter.selectedValue != null) {
                    var startDate = cboBIRCASSalesJournalStartDateFilter.value.toLocaleDateString().replace(/\//g, '-');
                    var endDate = cboBIRCASSalesJournalEndDateFilter.value.toLocaleDateString().replace(/\//g, '-');
                    var companyId = cboBIRCASSalesJournalCompanyFilter.selectedValue;
                    var branchId = cboBIRCASSalesJournalBranchFilter.selectedValue;

                    window.location.href = '/RepBIRCASSalesJournal/BIRCASSalesJournal?StartDate=' + startDate + '&EndDate=' + endDate + '&CompanyId=' + companyId + '&BranchId=' + branchId;
                } else {
                    toastr.error("Lacking Information");
                }
            } else {
                toastr.error("Lacking Information");
            }
        }

        // =============================
        // Document Ready - On load Page
        // =============================
        $(document).ready(function () {
            createCboDateRangedFilter();
        });
    </script>
    <script type="text/javascript">
        // =================
        // User Branch Combo
        // =================
        var cboUserBranches = new wijmo.input.ComboBox('#cboUserBranches');

        // =========================
        // Get Current User Branches
        // =========================
        function getUserBranches() {
            $("#loadingUserBranches").show();
            document.getElementById("loadingUserBranches").innerHTML = '<i class="fa fa-spinner fa-spin"></i>';
            $("#userBranchContent").hide();

            $('#userBranchesModal').modal({ show: true, backdrop: 'static' });

            document.getElementById('updateUserBranch').innerHTML = "<i class='fa fa-save fa-fw'></i> Update";
            $("#updateUserBranch").prop("disabled", false);
            $("#closeUserBranch").prop("disabled", false);

            var userBranchesObservableArray = new wijmo.collections.ObservableArray();
            $.ajax({
                url: '/api/userBranch/list',
                cache: false,
                type: 'GET',
                contentType: 'application/json; charset=utf-8',
                success: function (userBranchesResults) {
                    if (userBranchesResults.length > 0) {
                        for (i = 0; i < userBranchesResults.length; i++) {
                            userBranchesObservableArray.push({
                                BranchId: userBranchesResults[i]["BranchId"],
                                Branch: userBranchesResults[i]["Branch"]
                            });
                        }
                    }

                    var currentBranch = "@ViewData["BranchId"]";
                    cboUserBranches.dispose();
                    cboUserBranches = new wijmo.input.ComboBox('#cboUserBranches', {
                        isEditable: false,
                        placeholder: "Select Branches",
                        itemsSource: userBranchesObservableArray,
                        displayMemberPath: "Branch",
                        selectedValuePath: "BranchId",
                        selectedValue: currentBranch
                    });

                    setTimeout(function () {
                        $("#loadingUserBranches").hide();
                        $("#userBranchContent").show();
                    }, 500)
                }
            });
        }

        // ==========================
        // Update Current User Branch
        // ==========================
        function updateUserBranchOnclick() {
            document.getElementById('updateUserBranch').innerHTML = "<i class='fa fa-spinner fa-spin'></i> Updating";
            $("#updateUserBranch").prop("disabled", true);
            $("#closeUserBranch").prop("disabled", true);

            var userBranchObject = new Object();
            userBranchObject.BranchId = cboUserBranches.selectedValue;
            var userBranchData = JSON.stringify(userBranchObject);

            $.ajax({
                type: "PUT",
                url: '/api/userBranch/update',
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                data: userBranchData,
                statusCode: {
                    200: function () {
                        toastr.success("Update Successful");
                        window.setTimeout(function () {
                            location.reload()
                        }, 1000);
                    },
                    404: function () {
                        toastr.error("Not Found");
                        document.getElementById('updateUserBranch').innerHTML = "<i class='fa fa-save fa-fw'></i> Update";
                        $("#updateUserBranch").prop("disabled", false);
                        $("#closeUserBranch").prop("disabled", false);
                    },
                    500: function () {
                        toastr.error("Bad Request");
                        document.getElementById('updateUserBranch').innerHTML = "<i class='fa fa-save fa-fw'></i> Update";
                        $("#updateUserBranch").prop("disabled", false);
                        $("#closeUserBranch").prop("disabled", false);
                    }
                }
            });
        }
    </script>
    <script type="text/javascript">
        // ======================
        // Side Bar Menu Settings
        // ======================
        var slideLeft = new Menu({
            wrapper: '#o-wrapper',
            type: 'slide-left',
            menuOpenerClass: '.c-button',
            maskId: '#c-mask'
        });

        var slideLeftBtn = document.querySelector('#c-button--slide-left');

        slideLeftBtn.addEventListener('click', function (e) {
            e.preventDefault;
            slideLeft.open();
        });
    </script>
</body>
</html>
